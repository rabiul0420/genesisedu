<div>
    <input type="checkbox" id="exam_id_{{$topic_exam_list->id}}" name="exam_id" value="{{ $topic_exam_list->id }}" {{ $checked }}>
    <label id="label_{{$topic_exam_list->id}}" for="exam_id_{{$topic_exam_list->id}}">{{ $topic_exam_add_info }}</label>
</div>
