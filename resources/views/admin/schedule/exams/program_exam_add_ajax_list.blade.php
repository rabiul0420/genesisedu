<div>
    <input type="checkbox" id="topic_content_id_{{$program_exam_list->topic_content_id}}" name="topic_content_id" value="{{ $program_exam_list->topic_content_id }}" {{ $checked }}>
    <label id="label_{{$program_exam_list->topic_content_id}}" for="topic_content_id_{{$program_exam_list->topic_content_id}}">{{ $program_exam_add_info }}</label>
</div>
