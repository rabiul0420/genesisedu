
<div>
	{{-- <table class="table table-striped table-bordered table-hover datatable">
		
		<tbody>
            <tr>
                <td>From Batch</td>
                @foreach ($from_batches as $from_batch)
                    <td >{{$from_batch->name}}</td>
                @endforeach   
            </tr>
            <tr>
                <td>To Batch</td>
                @foreach ($to_batches as $to_batch)
                <td>{{$to_batch->name}}</td>
                @endforeach
            </tr>
			
		</tbody>
	</table> --}}
    <span>{{ $batch_shifted_info->batch_shifted_info }}</span>
</div> 